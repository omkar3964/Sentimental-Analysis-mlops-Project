from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project applies Machine Learning and MLOps practices to build, deploy, and monitor a sentiment analysis system. The goal is to classify text (such as customer reviews, tweets, or feedback) into categories like positive, negative, or neutral.',
    author='omkar',
    license='MIT',
)
